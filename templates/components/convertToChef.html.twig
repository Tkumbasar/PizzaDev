{% extends 'base.html.twig' %}

{% block body %}
    <h1>Convert User to Chef</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Convert</button>
    {{ form_end(form) }}
{% endblock %}